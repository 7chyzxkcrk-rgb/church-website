{
  "name": "church-website",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cp -r dist ../server/dist",
    "start": "cd server && node index.js"
  }
}
